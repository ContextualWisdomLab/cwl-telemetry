# CWL Telemetry

Shared runtime telemetry dependency for ContextualWisdomLab products.
Importing the package is inert. Applications must call `bootstrap()` to opt in.

Implementation is in progress under
[ContextualWisdomLab/.github#1565](https://github.com/ContextualWisdomLab/.github/issues/1565).
